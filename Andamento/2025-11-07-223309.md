# Atualização 2025-11-07 22:33:09

## Alterações

- node_helper do MMM-SensorData passou a memorizar o último snapshot meteorológico recebido via `SENSORDATA_WEATHER_UPDATE`.
- Prompt enviado para a IA agora inclui blocos estruturados com condições atuais, próximas horas e previsão diária quando há dados de meteorologia válidos.
- Ajustes de lint no helper (uso de Number.parseInt, replaceAll, checagens explícitas) para manter o arquivo em conformidade.
- Ajustado `jest.config.js` para mapear `node_helper`, ignorar os testes baseados em `node:test` do MMM-Remote-Control e normalizado final de linha.
- Preenchidos `package.json` mínimos para MMM-WinVoice/WinVoice2 e convertidos para nomes minúsculos.
- Corrigido `test_sequencer` para tolerar caminhos Windows e flexibilizado `utils_spec` para aceitar outras plataformas.

## Estado atual

- Cliente front-end já encaminha `SENSORDATA_WEATHER_UPDATE` e o backend consome/usa essas informações no prompt.
- Ainda não existe persistência longa de meteorologia nem exposição do payload no relatório enviado ao app; somente o prompt foi enriquecido.
- `npx jest -i --forceExit` agora roda os testes unitários; suítes electron ainda estouram timeout por falta de ambiente GUI.
